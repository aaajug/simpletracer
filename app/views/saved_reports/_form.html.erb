<%= form_with(model: saved_report) do |form| %>
  <% if saved_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saved_report.errors.count, "error") %> prohibited this saved_report from being saved:</h2>

      <ul>
        <% saved_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :caseDate %>
    <%= form.text_field :caseDate %>
  </div>

  <div class="field">
    <%= form.label :linkToFile %>
    <%= form.text_field :linkToFile %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
